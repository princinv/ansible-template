playbook/
├── ansible.cfg            # Global Ansible config (set roles_path, inventory, etc.)
├── inventory/             # Your inventory definition
│   ├── hosts              # Main inventory file (could be static, dynamic, etc.)
│   ├── group_vars/        # Variables common to groups of hosts
│   │   └── all.yml        # Variables applied to all hosts
│   └── host_vars/         # Variables specific to individual hosts
│       └── hostname.yml   # (example file for a host)
├── playbooks/             # Top-level playbook files
│   └── site.yml           # Main playbook entry point (could include others)
├── roles/                 # Reusable roles: self-contained units of configuration
│   ├── common/            # Example role (e.g., common configuration)
│   │   ├── tasks/         
│   │   │   └── main.yml   # Tasks to execute for this role
│   │   ├── handlers/      
│   │   │   └── main.yml   # Handlers (e.g., service restarts) for this role
│   │   ├── templates/     # Jinja2 templates to be deployed
│   │   ├── files/         # Static files to be copied
│   │   ├── vars/          
│   │   │   └── main.yml   # Variables with a higher precedence than defaults
│   │   ├── defaults/      
│   │   │   └── main.yml   # Role default variables (lowest priority)
│   │   ├── meta/          
│   │   │   └── main.yml   # Metadata and dependency information
│   │   └── README.md      # Role documentation
│   └── <other roles>/     # Other roles you might create (e.g., for specific services)
├── library/               # Custom modules if you write any (optional)
├── filter_plugins/        # Custom filter plugins (optional)
└── requirements.yml       # Role dependencies (for use with ansible-galaxy)
